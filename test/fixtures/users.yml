# Read about fixtures at http://api.rubyonrails.org/classes/ActiveRecord/FixtureSet.html

# This model initially had no columns defined.  If you add columns to the
# model remove the '{}' from the fixture names and add the columns immediately
# below each fixture, per the syntax in the comments below
#
#one: {} removed.
# column: value
#
#two: {} removed.
#  column: value

#FIXTURES: dummy objects created to be run as tests. they must be valid and be temporarly registered on a test database.

bob:
  name: Bob Bobby
  email: bob@bobbie.bob
  password_digest: <%= User.digest('h4ck3d') %> # the 'User.digest()' class method can be found on /models/user.rb 
